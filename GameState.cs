using System;

namespace SpaceInvasion
{
    public enum GameState
    {
        LOADING,
        SETUP,
        TITLE,
        LEVEL_START,
        LEVEL_PLAY,
        LEVEL_END,
        GAME_OVER
    }
}
